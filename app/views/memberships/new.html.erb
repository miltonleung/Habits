<% if user_signed_in? %>
    <% if current_user.habits.where(habit_id: @habit.id) %>
        Joined
    <% else %>
        <%= render 'form' %>
    <% end %>
<% end %>

<%= link_to 'Back', habits_path %>