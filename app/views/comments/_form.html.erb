<%= form_for([@habit, @habit.comments.build]) do |f| %>
  <p>
    <%= current_user.firstname %> <%= current_user.lastname %>
  </p>

  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>